# Metrics Guide

The authoritative metrics documentation now lives in:

- `docs/METRICS.md`

The older monitoring-specific guide was replaced to avoid stale metric names and
incorrect CPU/memory interpretation in paper references.
